(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory();
	else if(typeof define === 'function' && define.amd)
		define("GestureHelper", [], factory);
	else if(typeof exports === 'object')
		exports["GestureHelper"] = factory();
	else
		root["GestureHelper"] = factory();
})(this, function() {
return /******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, {
/******/ 				configurable: false,
/******/ 				enumerable: true,
/******/ 				get: getter
/******/ 			});
/******/ 		}
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "/";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 3);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports, __webpack_require__) {

/* WEBPACK VAR INJECTION */(function(process) {// Generated by CoffeeScript 1.12.2
(function() {
  var getNanoSeconds, hrtime, loadTime, moduleLoadTime, nodeLoadTime, upTime;

  if ((typeof performance !== "undefined" && performance !== null) && performance.now) {
    module.exports = function() {
      return performance.now();
    };
  } else if ((typeof process !== "undefined" && process !== null) && process.hrtime) {
    module.exports = function() {
      return (getNanoSeconds() - nodeLoadTime) / 1e6;
    };
    hrtime = process.hrtime;
    getNanoSeconds = function() {
      var hr;
      hr = hrtime();
      return hr[0] * 1e9 + hr[1];
    };
    moduleLoadTime = getNanoSeconds();
    upTime = process.uptime() * 1e9;
    nodeLoadTime = moduleLoadTime - upTime;
  } else if (Date.now) {
    module.exports = function() {
      return Date.now() - loadTime;
    };
    loadTime = Date.now();
  } else {
    module.exports = function() {
      return new Date().getTime() - loadTime;
    };
    loadTime = new Date().getTime();
  }

}).call(this);

//# sourceMappingURL=performance-now.js.map

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(1)))

/***/ }),
/* 1 */
/***/ (function(module, exports) {

// shim for using process in browser
var process = module.exports = {};

// cached from whatever global is present so that test runners that stub it
// don't break things.  But we need to wrap it in a try catch in case it is
// wrapped in strict mode code which doesn't define any globals.  It's inside a
// function because try/catches deoptimize in certain engines.

var cachedSetTimeout;
var cachedClearTimeout;

function defaultSetTimout() {
    throw new Error('setTimeout has not been defined');
}
function defaultClearTimeout () {
    throw new Error('clearTimeout has not been defined');
}
(function () {
    try {
        if (typeof setTimeout === 'function') {
            cachedSetTimeout = setTimeout;
        } else {
            cachedSetTimeout = defaultSetTimout;
        }
    } catch (e) {
        cachedSetTimeout = defaultSetTimout;
    }
    try {
        if (typeof clearTimeout === 'function') {
            cachedClearTimeout = clearTimeout;
        } else {
            cachedClearTimeout = defaultClearTimeout;
        }
    } catch (e) {
        cachedClearTimeout = defaultClearTimeout;
    }
} ())
function runTimeout(fun) {
    if (cachedSetTimeout === setTimeout) {
        //normal enviroments in sane situations
        return setTimeout(fun, 0);
    }
    // if setTimeout wasn't available but was latter defined
    if ((cachedSetTimeout === defaultSetTimout || !cachedSetTimeout) && setTimeout) {
        cachedSetTimeout = setTimeout;
        return setTimeout(fun, 0);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedSetTimeout(fun, 0);
    } catch(e){
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't trust the global object when called normally
            return cachedSetTimeout.call(null, fun, 0);
        } catch(e){
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error
            return cachedSetTimeout.call(this, fun, 0);
        }
    }


}
function runClearTimeout(marker) {
    if (cachedClearTimeout === clearTimeout) {
        //normal enviroments in sane situations
        return clearTimeout(marker);
    }
    // if clearTimeout wasn't available but was latter defined
    if ((cachedClearTimeout === defaultClearTimeout || !cachedClearTimeout) && clearTimeout) {
        cachedClearTimeout = clearTimeout;
        return clearTimeout(marker);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedClearTimeout(marker);
    } catch (e){
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't  trust the global object when called normally
            return cachedClearTimeout.call(null, marker);
        } catch (e){
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error.
            // Some versions of I.E. have different rules for clearTimeout vs setTimeout
            return cachedClearTimeout.call(this, marker);
        }
    }



}
var queue = [];
var draining = false;
var currentQueue;
var queueIndex = -1;

function cleanUpNextTick() {
    if (!draining || !currentQueue) {
        return;
    }
    draining = false;
    if (currentQueue.length) {
        queue = currentQueue.concat(queue);
    } else {
        queueIndex = -1;
    }
    if (queue.length) {
        drainQueue();
    }
}

function drainQueue() {
    if (draining) {
        return;
    }
    var timeout = runTimeout(cleanUpNextTick);
    draining = true;

    var len = queue.length;
    while(len) {
        currentQueue = queue;
        queue = [];
        while (++queueIndex < len) {
            if (currentQueue) {
                currentQueue[queueIndex].run();
            }
        }
        queueIndex = -1;
        len = queue.length;
    }
    currentQueue = null;
    draining = false;
    runClearTimeout(timeout);
}

process.nextTick = function (fun) {
    var args = new Array(arguments.length - 1);
    if (arguments.length > 1) {
        for (var i = 1; i < arguments.length; i++) {
            args[i - 1] = arguments[i];
        }
    }
    queue.push(new Item(fun, args));
    if (queue.length === 1 && !draining) {
        runTimeout(drainQueue);
    }
};

// v8 likes predictible objects
function Item(fun, array) {
    this.fun = fun;
    this.array = array;
}
Item.prototype.run = function () {
    this.fun.apply(null, this.array);
};
process.title = 'browser';
process.browser = true;
process.env = {};
process.argv = [];
process.version = ''; // empty string to avoid regexp issues
process.versions = {};

function noop() {}

process.on = noop;
process.addListener = noop;
process.once = noop;
process.off = noop;
process.removeListener = noop;
process.removeAllListeners = noop;
process.emit = noop;
process.prependListener = noop;
process.prependOnceListener = noop;

process.listeners = function (name) { return [] }

process.binding = function (name) {
    throw new Error('process.binding is not supported');
};

process.cwd = function () { return '/' };
process.chdir = function (dir) {
    throw new Error('process.chdir is not supported');
};
process.umask = function() { return 0; };


/***/ }),
/* 2 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;

var _eventemitter = __webpack_require__(7);

var _eventemitter2 = _interopRequireDefault(_eventemitter);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var ee = new _eventemitter2.default({
  wildcard: true
});

exports.default = ee;

/***/ }),
/* 3 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;

var _performanceNow = __webpack_require__(0);

var _performanceNow2 = _interopRequireDefault(_performanceNow);

var _momentum = __webpack_require__(4);

var _momentum2 = _interopRequireDefault(_momentum);

var _emitter = __webpack_require__(2);

var _emitter2 = _interopRequireDefault(_emitter);

var _inputdevicecapabilitiesPolyfill = __webpack_require__(8);

var _inputdevicecapabilitiesPolyfill2 = _interopRequireDefault(_inputdevicecapabilitiesPolyfill);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

(0, _inputdevicecapabilitiesPolyfill2.default)(window);

// @TODO: the eventEmitter related stuff should be split out,
// and not so merged with the rest of this lib...


// @TODO: temp polyfill code, should tidy this a bit..
function GestureHelper() {
  var _this = this;

  var props = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  var _props$el = props.el,
      el = _props$el === undefined ? null : _props$el,
      _props$sensitivity = props.sensitivity,
      sensitivity = _props$sensitivity === undefined ? 5 : _props$sensitivity,
      _props$passive = props.passive,
      passive = _props$passive === undefined ? false : _props$passive,
      _props$capture = props.capture,
      capture = _props$capture === undefined ? false : _props$capture,
      _props$swipeVelocity = props.swipeVelocity,
      swipeVelocity = _props$swipeVelocity === undefined ? 0.7 : _props$swipeVelocity,
      _props$maxTapDuration = props.maxTapDuration,
      maxTapDuration = _props$maxTapDuration === undefined ? 300 : _props$maxTapDuration,
      _props$longTapDuratio = props.longTapDuration,
      longTapDuration = _props$longTapDuratio === undefined ? 400 : _props$longTapDuratio,
      _props$startDirection = props.startDirectionLoopCount,
      startDirectionLoopCount = _props$startDirection === undefined ? 2 : _props$startDirection;


  console.log(this);
  var velocity = void 0,
      eventOptions = void 0;
  var clearVelocityStats = function clearVelocityStats() {
    _this.velocity = {
      current: { x: 0, y: 0 },
      max: { x: 0, y: 0 }
    };
  };

  clearVelocityStats();
  setup();

  var isPanning = function isPanning() {
    return _this.panning;
  };

  var setup = function setup() {
    el.addEventListener('mousedown', mouseDown, eventOptions);
    el.addEventListener('mouseup', mouseUp, eventOptions);
    el.addEventListener('touchstart', touchStart, eventOptions);
    el.addEventListener('touchend', touchEnd, eventOptions);
    el.addEventListener('touchcancel', touchEnd, eventOptions);

    // Small feature detect for support of "passive" events
    var eventOptions = false;
    try {
      var options = Object.defineProperty({}, "passive", {
        get: function get() {
          eventOptions = {
            passive: !!passive,
            capture: !!capture
          };
        }
      });
      el.addEventListener("test", null, options);
    } catch (err) {
      console.error(err);
      eventOptions = {
        capture: !!capture
      };
    }
  };

  var mouseMove = function mouseMove(e) {
    handleMove({ e: e, x: e.clientX, y: e.clientY });
  };

  var touchMove = function touchMove(e) {
    handleMove({ e: e, x: e.touches[0].clientX, y: e.touches[0].clientY });
  };

  var touchStart = function touchStart(e) {
    _this.handleStart({
      x: e.touches[0].clientX,
      y: e.touches[0].clientY,
      e: e
    });
    el.addEventListener('touchmove', touchMove, eventOptions);
  };

  var touchEnd = function touchEnd(e) {
    handleEnd(e);
    el.removeEventListener('touchmove', touchMove, eventOptions);
  };

  var mouseDown = function mouseDown(e) {
    if (e.sourceCapabilities.firesTouchEvents) return;
    _this.handleStart({ x: e.clientX, y: e.clientY, e: e });
    el.addEventListener('mousemove', mouseMove, eventOptions);
  };

  var mouseUp = function mouseUp(e) {
    if (e.sourceCapabilities.firesTouchEvents) return;
    _this.handleEnd(e);
    el.removeEventListener('mousemove', mouseMove, eventOptions);
  };

  var destroy = function destroy() {
    el.removeEventListener('mousedown', mouseDown, eventOptions);
    el.removeEventListener('mouseup', mouseUp, eventOptions);
    el.removeEventListener('mousemove', mouseMove, eventOptions);
    el.removeEventListener('touchstart', touchStart, eventOptions);
    el.removeEventListener('touchend', touchEnd, eventOptions);
    el.removeEventListener('touchcancel', touchEnd, eventOptions);
    el.removeEventListener('touchmove', touchMove, eventOptions);
  };

  var getStartDirection = function getStartDirection(_ref) {
    var _ref$x = _ref.x,
        x = _ref$x === undefined ? 0 : _ref$x,
        _ref$y = _ref.y,
        y = _ref$y === undefined ? 0 : _ref$y;

    if (_this.directionCount <= _this.options.startDirectionLoopCount) {
      _this.directionCount++;
      return null;
    } else {
      return Math.abs(x) > Math.abs(y) ? 'horizontal' : 'vertical';
    }
  };

  var handleStart = function handleStart(_ref2) {
    var _ref2$x = _ref2.x,
        x = _ref2$x === undefined ? 0 : _ref2$x,
        _ref2$y = _ref2.y,
        y = _ref2$y === undefined ? 0 : _ref2$y,
        _ref2$e = _ref2.e,
        e = _ref2$e === undefined ? {} : _ref2$e;


    // Ensure all settings are reset:
    _this.startX = x;
    _this.startY = y;
    _this.startDirection = null;
    _this.directionCount = 1;
    _this.panning = false;
    _this.startTime = (0, _performanceNow2.default)();
    clearVelocityStats();
    _emitter2.default.emit('pan.prestart', { sourceEvent: e });
  };

  var handleMove = function handleMove(_ref3) {
    var _ref3$e = _ref3.e,
        e = _ref3$e === undefined ? {} : _ref3$e,
        _ref3$x = _ref3.x,
        x = _ref3$x === undefined ? 0 : _ref3$x,
        _ref3$y = _ref3.y,
        y = _ref3$y === undefined ? 0 : _ref3$y;

    var deltaX = x - _this.startX;
    var deltaY = y - _this.startY;

    if (_this.startDirection === null) {
      _this.startDirection = _this.getStartDirection({ x: deltaX, y: deltaY });
    } else if (!_this.panning && (Math.abs(deltaX) > _this.options.sensitivity || Math.abs(deltaY) > _this.options.sensitivity)) {

      _this.panning = true;
      _emitter2.default.emit('pan.start', {
        startDirection: _this.startDirection,
        sourceEvent: e
      });
    }

    if (_this.panning) {
      _emitter2.default.emit('pan.all', {
        startDirection: _this.startDirection,
        deltaX: deltaX, deltaY: deltaY,
        sourceEvent: e
      });

      if (_this.startDirection === 'horizontal') {
        deltaX < 0;
        _emitter2.default.emit('pan.x.left', { delta: deltaX, sourceEvent: e });
        _emitter2.default.emit('pan.x.right', { delta: deltaX, sourceEvent: e });
      } else if (_this.startDirection === 'vertical') {
        deltaY < 0;
        _emitter2.default.emit('pan.y.up', { delta: deltaY, sourceEvent: e });
        _emitter2.default.emit('pan.y.down', { delta: deltaY, sourceEvent: e });
      }

      // velocity = total distance moved / the time taken
      var deltaTime = (0, _performanceNow2.default)() - _this.startTime;
      _this.velocity.current.x = deltaX / deltaTime * 100;
      _this.velocity.current.y = deltaY / deltaTime * 100;
      _this.velocity.max.x = Math.max(_this.velocity.max.x, Math.abs(_this.velocity.current.x));
      _this.velocity.max.y = Math.max(_this.velocity.max.y, Math.abs(_this.velocity.current.y));
      _this.lastDeltaX = deltaX;
      _this.lastDeltaY = deltaY;
    }
  };

  var handleEnd = function handleEnd(e) {
    var deltaTime = (0, _performanceNow2.default)() - _this.startTime;
    if (_this.panning) {
      _this.panning = false;
      var isSwipe = false;
      var swipeDirection = null;
      if (_this.velocity.max.x > _this.options.swipeVelocity && _this.startDirection === 'horizontal') {

        isSwipe = true;
        swipeDirection = _this.velocity.current.x > 0 ? 'right' : 'left';
      } else if (_this.velocity.max.y > _this.options.swipeVelocity && _this.startDirection === 'vertical') {

        isSwipe = true;
        swipeDirection = _this.velocity.current.y > 0 ? 'down' : 'up';
      }
      _emitter2.default.emit('pan.end', { isSwipe: isSwipe, swipeDirection: swipeDirection, sourceEvent: e });
      if (_this.options.useMomentum) {
        var direction = Math.abs(_this.velocity.current.x) > Math.abs(_this.velocity.current.y) ? 'horizontal' : 'vertical';

        _this.momentum = (0, _momentum2.default)({
          velocity: {
            x: _this.velocity.current.x,
            y: _this.velocity.current.y
          },
          from: {
            x: _this.lastDeltaX,
            y: _this.lastDeltaY
          }
        });
        _this.momentum.start();
      }
    } else if (deltaTime <= _this.options.maxTapDuration) {
      _emitter2.default.emit('tap', { srcEvent: e });
    }

    return;
  };

  return _emitter2.default;
}

exports.default = GestureHelper;

/***/ }),
/* 4 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;

var _raf = __webpack_require__(5);

var _raf2 = _interopRequireDefault(_raf);

var _performanceNow = __webpack_require__(0);

var _performanceNow2 = _interopRequireDefault(_performanceNow);

var _emitter = __webpack_require__(2);

var _emitter2 = _interopRequireDefault(_emitter);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var _modifyTarget = function modifyTarget(idealTarget) {
  // @TODO: allow the momentum to be rounded down to the nearest integer:
  return idealTarget;
}; // Reimplementing:
// github.com/Popmotion/popmotion/blob/master/packages/popmotion/src/animations/decay/index.ts

var momentum = function momentum() {
  var modifyTarget = _modifyTarget;
  var props = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};

  var _props$velocity = props.velocity,
      velocity = _props$velocity === undefined ? { x: 0, y: 0 } : _props$velocity,
      _props$from = props.from,
      from = _props$from === undefined ? { x: 0, y: 0 } : _props$from,
      _props$power = props.power,
      power = _props$power === undefined ? 0.8 : _props$power,
      _props$timeConstant = props.timeConstant,
      timeConstant = _props$timeConstant === undefined ? 350 : _props$timeConstant,
      _props$restDelta = props.restDelta,
      restDelta = _props$restDelta === undefined ? 0.5 : _props$restDelta,
      roundDestination = props.roundDestination,
      emitter = props.emitter,
      _hoistedAnonymousFunc = function _hoistedAnonymousFunc() {
    elapsed = (0, _performanceNow2.default)() - timeStart;
    var delta = -amplitude * Math.exp(-elapsed / timeConstant);
    var isMoving = delta > restDelta || delta < -restDelta;
    var current = isMoving ? target + delta : target;

    console.log('momentum update ---------------------\n        from: ' + from + ',\n        velocity:' + velocity + ',\n        amplitude: ' + amplitude + ',\n        target: ' + target + ',\n        elapsed: ' + elapsed + ',\n        delta: ' + delta + ',\n        current: ' + current + ',\n      ');

    console.log('emitting!');
    emitter.emit('momentum.all', { current: current });

    // Loop back, or complete the momentum cycle:
    if (isMoving) {
      update();
    } else {
      console.log('stopping!');
      emitter.emit('momentum.complete', {});
    }
  };

  var elapsed = 0;
  var rafIndex = null;

  var amplitude = power * velocity;
  var timeStart = (0, _performanceNow2.default)();
  var idealTarget = Math.round(from + amplitude);
  var target = typeof modifyTarget === 'undefined' ? idealTarget : modifyTarget(idealTarget);

  function update() {
    rafIndex = (0, _raf2.default)(_hoistedAnonymousFunc);
  }

  return {
    start: update,
    stop: function stop() {
      return isMoving = false;
    }
  };
};

exports.default = momentum;

/***/ }),
/* 5 */
/***/ (function(module, exports, __webpack_require__) {

/* WEBPACK VAR INJECTION */(function(global) {var now = __webpack_require__(0)
  , root = typeof window === 'undefined' ? global : window
  , vendors = ['moz', 'webkit']
  , suffix = 'AnimationFrame'
  , raf = root['request' + suffix]
  , caf = root['cancel' + suffix] || root['cancelRequest' + suffix]

for(var i = 0; !raf && i < vendors.length; i++) {
  raf = root[vendors[i] + 'Request' + suffix]
  caf = root[vendors[i] + 'Cancel' + suffix]
      || root[vendors[i] + 'CancelRequest' + suffix]
}

// Some versions of FF have rAF but not cAF
if(!raf || !caf) {
  var last = 0
    , id = 0
    , queue = []
    , frameDuration = 1000 / 60

  raf = function(callback) {
    if(queue.length === 0) {
      var _now = now()
        , next = Math.max(0, frameDuration - (_now - last))
      last = next + _now
      setTimeout(function() {
        var cp = queue.slice(0)
        // Clear queue here to prevent
        // callbacks from appending listeners
        // to the current frame's queue
        queue.length = 0
        for(var i = 0; i < cp.length; i++) {
          if(!cp[i].cancelled) {
            try{
              cp[i].callback(last)
            } catch(e) {
              setTimeout(function() { throw e }, 0)
            }
          }
        }
      }, Math.round(next))
    }
    queue.push({
      handle: ++id,
      callback: callback,
      cancelled: false
    })
    return id
  }

  caf = function(handle) {
    for(var i = 0; i < queue.length; i++) {
      if(queue[i].handle === handle) {
        queue[i].cancelled = true
      }
    }
  }
}

module.exports = function(fn) {
  // Wrap in a new function to prevent
  // `cancel` potentially being assigned
  // to the native rAF function
  return raf.call(root, fn)
}
module.exports.cancel = function() {
  caf.apply(root, arguments)
}
module.exports.polyfill = function(object) {
  if (!object) {
    object = root;
  }
  object.requestAnimationFrame = raf
  object.cancelAnimationFrame = caf
}

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(6)))

/***/ }),
/* 6 */
/***/ (function(module, exports) {

var g;

// This works in non-strict mode
g = (function() {
	return this;
})();

try {
	// This works if eval is allowed (see CSP)
	g = g || Function("return this")() || (1,eval)("this");
} catch(e) {
	// This works if the window reference is available
	if(typeof window === "object")
		g = window;
}

// g can still be undefined, but nothing to do about it...
// We return undefined, instead of nothing here, so it's
// easier to handle this case. if(!global) { ...}

module.exports = g;


/***/ }),
/* 7 */
/***/ (function(module, exports, __webpack_require__) {

/* WEBPACK VAR INJECTION */(function(process) {var __WEBPACK_AMD_DEFINE_RESULT__;/*!
 * EventEmitter2
 * https://github.com/hij1nx/EventEmitter2
 *
 * Copyright (c) 2013 hij1nx
 * Licensed under the MIT license.
 */
;!function(undefined) {

  var isArray = Array.isArray ? Array.isArray : function _isArray(obj) {
    return Object.prototype.toString.call(obj) === "[object Array]";
  };
  var defaultMaxListeners = 10;

  function init() {
    this._events = {};
    if (this._conf) {
      configure.call(this, this._conf);
    }
  }

  function configure(conf) {
    if (conf) {
      this._conf = conf;

      conf.delimiter && (this.delimiter = conf.delimiter);
      this._maxListeners = conf.maxListeners !== undefined ? conf.maxListeners : defaultMaxListeners;

      conf.wildcard && (this.wildcard = conf.wildcard);
      conf.newListener && (this._newListener = conf.newListener);
      conf.removeListener && (this._removeListener = conf.removeListener);
      conf.verboseMemoryLeak && (this.verboseMemoryLeak = conf.verboseMemoryLeak);

      if (this.wildcard) {
        this.listenerTree = {};
      }
    } else {
      this._maxListeners = defaultMaxListeners;
    }
  }

  function logPossibleMemoryLeak(count, eventName) {
    var errorMsg = '(node) warning: possible EventEmitter memory ' +
        'leak detected. ' + count + ' listeners added. ' +
        'Use emitter.setMaxListeners() to increase limit.';

    if(this.verboseMemoryLeak){
      errorMsg += ' Event name: ' + eventName + '.';
    }

    if(typeof process !== 'undefined' && process.emitWarning){
      var e = new Error(errorMsg);
      e.name = 'MaxListenersExceededWarning';
      e.emitter = this;
      e.count = count;
      process.emitWarning(e);
    } else {
      console.error(errorMsg);

      if (console.trace){
        console.trace();
      }
    }
  }

  function EventEmitter(conf) {
    this._events = {};
    this._newListener = false;
    this._removeListener = false;
    this.verboseMemoryLeak = false;
    configure.call(this, conf);
  }
  EventEmitter.EventEmitter2 = EventEmitter; // backwards compatibility for exporting EventEmitter property

  //
  // Attention, function return type now is array, always !
  // It has zero elements if no any matches found and one or more
  // elements (leafs) if there are matches
  //
  function searchListenerTree(handlers, type, tree, i) {
    if (!tree) {
      return [];
    }
    var listeners=[], leaf, len, branch, xTree, xxTree, isolatedBranch, endReached,
        typeLength = type.length, currentType = type[i], nextType = type[i+1];
    if (i === typeLength && tree._listeners) {
      //
      // If at the end of the event(s) list and the tree has listeners
      // invoke those listeners.
      //
      if (typeof tree._listeners === 'function') {
        handlers && handlers.push(tree._listeners);
        return [tree];
      } else {
        for (leaf = 0, len = tree._listeners.length; leaf < len; leaf++) {
          handlers && handlers.push(tree._listeners[leaf]);
        }
        return [tree];
      }
    }

    if ((currentType === '*' || currentType === '**') || tree[currentType]) {
      //
      // If the event emitted is '*' at this part
      // or there is a concrete match at this patch
      //
      if (currentType === '*') {
        for (branch in tree) {
          if (branch !== '_listeners' && tree.hasOwnProperty(branch)) {
            listeners = listeners.concat(searchListenerTree(handlers, type, tree[branch], i+1));
          }
        }
        return listeners;
      } else if(currentType === '**') {
        endReached = (i+1 === typeLength || (i+2 === typeLength && nextType === '*'));
        if(endReached && tree._listeners) {
          // The next element has a _listeners, add it to the handlers.
          listeners = listeners.concat(searchListenerTree(handlers, type, tree, typeLength));
        }

        for (branch in tree) {
          if (branch !== '_listeners' && tree.hasOwnProperty(branch)) {
            if(branch === '*' || branch === '**') {
              if(tree[branch]._listeners && !endReached) {
                listeners = listeners.concat(searchListenerTree(handlers, type, tree[branch], typeLength));
              }
              listeners = listeners.concat(searchListenerTree(handlers, type, tree[branch], i));
            } else if(branch === nextType) {
              listeners = listeners.concat(searchListenerTree(handlers, type, tree[branch], i+2));
            } else {
              // No match on this one, shift into the tree but not in the type array.
              listeners = listeners.concat(searchListenerTree(handlers, type, tree[branch], i));
            }
          }
        }
        return listeners;
      }

      listeners = listeners.concat(searchListenerTree(handlers, type, tree[currentType], i+1));
    }

    xTree = tree['*'];
    if (xTree) {
      //
      // If the listener tree will allow any match for this part,
      // then recursively explore all branches of the tree
      //
      searchListenerTree(handlers, type, xTree, i+1);
    }

    xxTree = tree['**'];
    if(xxTree) {
      if(i < typeLength) {
        if(xxTree._listeners) {
          // If we have a listener on a '**', it will catch all, so add its handler.
          searchListenerTree(handlers, type, xxTree, typeLength);
        }

        // Build arrays of matching next branches and others.
        for(branch in xxTree) {
          if(branch !== '_listeners' && xxTree.hasOwnProperty(branch)) {
            if(branch === nextType) {
              // We know the next element will match, so jump twice.
              searchListenerTree(handlers, type, xxTree[branch], i+2);
            } else if(branch === currentType) {
              // Current node matches, move into the tree.
              searchListenerTree(handlers, type, xxTree[branch], i+1);
            } else {
              isolatedBranch = {};
              isolatedBranch[branch] = xxTree[branch];
              searchListenerTree(handlers, type, { '**': isolatedBranch }, i+1);
            }
          }
        }
      } else if(xxTree._listeners) {
        // We have reached the end and still on a '**'
        searchListenerTree(handlers, type, xxTree, typeLength);
      } else if(xxTree['*'] && xxTree['*']._listeners) {
        searchListenerTree(handlers, type, xxTree['*'], typeLength);
      }
    }

    return listeners;
  }

  function growListenerTree(type, listener) {

    type = typeof type === 'string' ? type.split(this.delimiter) : type.slice();

    //
    // Looks for two consecutive '**', if so, don't add the event at all.
    //
    for(var i = 0, len = type.length; i+1 < len; i++) {
      if(type[i] === '**' && type[i+1] === '**') {
        return;
      }
    }

    var tree = this.listenerTree;
    var name = type.shift();

    while (name !== undefined) {

      if (!tree[name]) {
        tree[name] = {};
      }

      tree = tree[name];

      if (type.length === 0) {

        if (!tree._listeners) {
          tree._listeners = listener;
        }
        else {
          if (typeof tree._listeners === 'function') {
            tree._listeners = [tree._listeners];
          }

          tree._listeners.push(listener);

          if (
            !tree._listeners.warned &&
            this._maxListeners > 0 &&
            tree._listeners.length > this._maxListeners
          ) {
            tree._listeners.warned = true;
            logPossibleMemoryLeak.call(this, tree._listeners.length, name);
          }
        }
        return true;
      }
      name = type.shift();
    }
    return true;
  }

  // By default EventEmitters will print a warning if more than
  // 10 listeners are added to it. This is a useful default which
  // helps finding memory leaks.
  //
  // Obviously not all Emitters should be limited to 10. This function allows
  // that to be increased. Set to zero for unlimited.

  EventEmitter.prototype.delimiter = '.';

  EventEmitter.prototype.setMaxListeners = function(n) {
    if (n !== undefined) {
      this._maxListeners = n;
      if (!this._conf) this._conf = {};
      this._conf.maxListeners = n;
    }
  };

  EventEmitter.prototype.event = '';


  EventEmitter.prototype.once = function(event, fn) {
    return this._once(event, fn, false);
  };

  EventEmitter.prototype.prependOnceListener = function(event, fn) {
    return this._once(event, fn, true);
  };

  EventEmitter.prototype._once = function(event, fn, prepend) {
    this._many(event, 1, fn, prepend);
    return this;
  };

  EventEmitter.prototype.many = function(event, ttl, fn) {
    return this._many(event, ttl, fn, false);
  }

  EventEmitter.prototype.prependMany = function(event, ttl, fn) {
    return this._many(event, ttl, fn, true);
  }

  EventEmitter.prototype._many = function(event, ttl, fn, prepend) {
    var self = this;

    if (typeof fn !== 'function') {
      throw new Error('many only accepts instances of Function');
    }

    function listener() {
      if (--ttl === 0) {
        self.off(event, listener);
      }
      return fn.apply(this, arguments);
    }

    listener._origin = fn;

    this._on(event, listener, prepend);

    return self;
  };

  EventEmitter.prototype.emit = function() {

    this._events || init.call(this);

    var type = arguments[0];

    if (type === 'newListener' && !this._newListener) {
      if (!this._events.newListener) {
        return false;
      }
    }

    var al = arguments.length;
    var args,l,i,j;
    var handler;

    if (this._all && this._all.length) {
      handler = this._all.slice();
      if (al > 3) {
        args = new Array(al);
        for (j = 0; j < al; j++) args[j] = arguments[j];
      }

      for (i = 0, l = handler.length; i < l; i++) {
        this.event = type;
        switch (al) {
        case 1:
          handler[i].call(this, type);
          break;
        case 2:
          handler[i].call(this, type, arguments[1]);
          break;
        case 3:
          handler[i].call(this, type, arguments[1], arguments[2]);
          break;
        default:
          handler[i].apply(this, args);
        }
      }
    }

    if (this.wildcard) {
      handler = [];
      var ns = typeof type === 'string' ? type.split(this.delimiter) : type.slice();
      searchListenerTree.call(this, handler, ns, this.listenerTree, 0);
    } else {
      handler = this._events[type];
      if (typeof handler === 'function') {
        this.event = type;
        switch (al) {
        case 1:
          handler.call(this);
          break;
        case 2:
          handler.call(this, arguments[1]);
          break;
        case 3:
          handler.call(this, arguments[1], arguments[2]);
          break;
        default:
          args = new Array(al - 1);
          for (j = 1; j < al; j++) args[j - 1] = arguments[j];
          handler.apply(this, args);
        }
        return true;
      } else if (handler) {
        // need to make copy of handlers because list can change in the middle
        // of emit call
        handler = handler.slice();
      }
    }

    if (handler && handler.length) {
      if (al > 3) {
        args = new Array(al - 1);
        for (j = 1; j < al; j++) args[j - 1] = arguments[j];
      }
      for (i = 0, l = handler.length; i < l; i++) {
        this.event = type;
        switch (al) {
        case 1:
          handler[i].call(this);
          break;
        case 2:
          handler[i].call(this, arguments[1]);
          break;
        case 3:
          handler[i].call(this, arguments[1], arguments[2]);
          break;
        default:
          handler[i].apply(this, args);
        }
      }
      return true;
    } else if (!this._all && type === 'error') {
      if (arguments[1] instanceof Error) {
        throw arguments[1]; // Unhandled 'error' event
      } else {
        throw new Error("Uncaught, unspecified 'error' event.");
      }
      return false;
    }

    return !!this._all;
  };

  EventEmitter.prototype.emitAsync = function() {

    this._events || init.call(this);

    var type = arguments[0];

    if (type === 'newListener' && !this._newListener) {
        if (!this._events.newListener) { return Promise.resolve([false]); }
    }

    var promises= [];

    var al = arguments.length;
    var args,l,i,j;
    var handler;

    if (this._all) {
      if (al > 3) {
        args = new Array(al);
        for (j = 1; j < al; j++) args[j] = arguments[j];
      }
      for (i = 0, l = this._all.length; i < l; i++) {
        this.event = type;
        switch (al) {
        case 1:
          promises.push(this._all[i].call(this, type));
          break;
        case 2:
          promises.push(this._all[i].call(this, type, arguments[1]));
          break;
        case 3:
          promises.push(this._all[i].call(this, type, arguments[1], arguments[2]));
          break;
        default:
          promises.push(this._all[i].apply(this, args));
        }
      }
    }

    if (this.wildcard) {
      handler = [];
      var ns = typeof type === 'string' ? type.split(this.delimiter) : type.slice();
      searchListenerTree.call(this, handler, ns, this.listenerTree, 0);
    } else {
      handler = this._events[type];
    }

    if (typeof handler === 'function') {
      this.event = type;
      switch (al) {
      case 1:
        promises.push(handler.call(this));
        break;
      case 2:
        promises.push(handler.call(this, arguments[1]));
        break;
      case 3:
        promises.push(handler.call(this, arguments[1], arguments[2]));
        break;
      default:
        args = new Array(al - 1);
        for (j = 1; j < al; j++) args[j - 1] = arguments[j];
        promises.push(handler.apply(this, args));
      }
    } else if (handler && handler.length) {
      handler = handler.slice();
      if (al > 3) {
        args = new Array(al - 1);
        for (j = 1; j < al; j++) args[j - 1] = arguments[j];
      }
      for (i = 0, l = handler.length; i < l; i++) {
        this.event = type;
        switch (al) {
        case 1:
          promises.push(handler[i].call(this));
          break;
        case 2:
          promises.push(handler[i].call(this, arguments[1]));
          break;
        case 3:
          promises.push(handler[i].call(this, arguments[1], arguments[2]));
          break;
        default:
          promises.push(handler[i].apply(this, args));
        }
      }
    } else if (!this._all && type === 'error') {
      if (arguments[1] instanceof Error) {
        return Promise.reject(arguments[1]); // Unhandled 'error' event
      } else {
        return Promise.reject("Uncaught, unspecified 'error' event.");
      }
    }

    return Promise.all(promises);
  };

  EventEmitter.prototype.on = function(type, listener) {
    return this._on(type, listener, false);
  };

  EventEmitter.prototype.prependListener = function(type, listener) {
    return this._on(type, listener, true);
  };

  EventEmitter.prototype.onAny = function(fn) {
    return this._onAny(fn, false);
  };

  EventEmitter.prototype.prependAny = function(fn) {
    return this._onAny(fn, true);
  };

  EventEmitter.prototype.addListener = EventEmitter.prototype.on;

  EventEmitter.prototype._onAny = function(fn, prepend){
    if (typeof fn !== 'function') {
      throw new Error('onAny only accepts instances of Function');
    }

    if (!this._all) {
      this._all = [];
    }

    // Add the function to the event listener collection.
    if(prepend){
      this._all.unshift(fn);
    }else{
      this._all.push(fn);
    }

    return this;
  }

  EventEmitter.prototype._on = function(type, listener, prepend) {
    if (typeof type === 'function') {
      this._onAny(type, listener);
      return this;
    }

    if (typeof listener !== 'function') {
      throw new Error('on only accepts instances of Function');
    }
    this._events || init.call(this);

    // To avoid recursion in the case that type == "newListeners"! Before
    // adding it to the listeners, first emit "newListeners".
    if (this._newListener)
       this.emit('newListener', type, listener);

    if (this.wildcard) {
      growListenerTree.call(this, type, listener);
      return this;
    }

    if (!this._events[type]) {
      // Optimize the case of one listener. Don't need the extra array object.
      this._events[type] = listener;
    }
    else {
      if (typeof this._events[type] === 'function') {
        // Change to array.
        this._events[type] = [this._events[type]];
      }

      // If we've already got an array, just add
      if(prepend){
        this._events[type].unshift(listener);
      }else{
        this._events[type].push(listener);
      }

      // Check for listener leak
      if (
        !this._events[type].warned &&
        this._maxListeners > 0 &&
        this._events[type].length > this._maxListeners
      ) {
        this._events[type].warned = true;
        logPossibleMemoryLeak.call(this, this._events[type].length, type);
      }
    }

    return this;
  }

  EventEmitter.prototype.off = function(type, listener) {
    if (typeof listener !== 'function') {
      throw new Error('removeListener only takes instances of Function');
    }

    var handlers,leafs=[];

    if(this.wildcard) {
      var ns = typeof type === 'string' ? type.split(this.delimiter) : type.slice();
      leafs = searchListenerTree.call(this, null, ns, this.listenerTree, 0);
    }
    else {
      // does not use listeners(), so no side effect of creating _events[type]
      if (!this._events[type]) return this;
      handlers = this._events[type];
      leafs.push({_listeners:handlers});
    }

    for (var iLeaf=0; iLeaf<leafs.length; iLeaf++) {
      var leaf = leafs[iLeaf];
      handlers = leaf._listeners;
      if (isArray(handlers)) {

        var position = -1;

        for (var i = 0, length = handlers.length; i < length; i++) {
          if (handlers[i] === listener ||
            (handlers[i].listener && handlers[i].listener === listener) ||
            (handlers[i]._origin && handlers[i]._origin === listener)) {
            position = i;
            break;
          }
        }

        if (position < 0) {
          continue;
        }

        if(this.wildcard) {
          leaf._listeners.splice(position, 1);
        }
        else {
          this._events[type].splice(position, 1);
        }

        if (handlers.length === 0) {
          if(this.wildcard) {
            delete leaf._listeners;
          }
          else {
            delete this._events[type];
          }
        }
        if (this._removeListener)
          this.emit("removeListener", type, listener);

        return this;
      }
      else if (handlers === listener ||
        (handlers.listener && handlers.listener === listener) ||
        (handlers._origin && handlers._origin === listener)) {
        if(this.wildcard) {
          delete leaf._listeners;
        }
        else {
          delete this._events[type];
        }
        if (this._removeListener)
          this.emit("removeListener", type, listener);
      }
    }

    function recursivelyGarbageCollect(root) {
      if (root === undefined) {
        return;
      }
      var keys = Object.keys(root);
      for (var i in keys) {
        var key = keys[i];
        var obj = root[key];
        if ((obj instanceof Function) || (typeof obj !== "object") || (obj === null))
          continue;
        if (Object.keys(obj).length > 0) {
          recursivelyGarbageCollect(root[key]);
        }
        if (Object.keys(obj).length === 0) {
          delete root[key];
        }
      }
    }
    recursivelyGarbageCollect(this.listenerTree);

    return this;
  };

  EventEmitter.prototype.offAny = function(fn) {
    var i = 0, l = 0, fns;
    if (fn && this._all && this._all.length > 0) {
      fns = this._all;
      for(i = 0, l = fns.length; i < l; i++) {
        if(fn === fns[i]) {
          fns.splice(i, 1);
          if (this._removeListener)
            this.emit("removeListenerAny", fn);
          return this;
        }
      }
    } else {
      fns = this._all;
      if (this._removeListener) {
        for(i = 0, l = fns.length; i < l; i++)
          this.emit("removeListenerAny", fns[i]);
      }
      this._all = [];
    }
    return this;
  };

  EventEmitter.prototype.removeListener = EventEmitter.prototype.off;

  EventEmitter.prototype.removeAllListeners = function(type) {
    if (type === undefined) {
      !this._events || init.call(this);
      return this;
    }

    if (this.wildcard) {
      var ns = typeof type === 'string' ? type.split(this.delimiter) : type.slice();
      var leafs = searchListenerTree.call(this, null, ns, this.listenerTree, 0);

      for (var iLeaf=0; iLeaf<leafs.length; iLeaf++) {
        var leaf = leafs[iLeaf];
        leaf._listeners = null;
      }
    }
    else if (this._events) {
      this._events[type] = null;
    }
    return this;
  };

  EventEmitter.prototype.listeners = function(type) {
    if (this.wildcard) {
      var handlers = [];
      var ns = typeof type === 'string' ? type.split(this.delimiter) : type.slice();
      searchListenerTree.call(this, handlers, ns, this.listenerTree, 0);
      return handlers;
    }

    this._events || init.call(this);

    if (!this._events[type]) this._events[type] = [];
    if (!isArray(this._events[type])) {
      this._events[type] = [this._events[type]];
    }
    return this._events[type];
  };

  EventEmitter.prototype.eventNames = function(){
    return Object.keys(this._events);
  }

  EventEmitter.prototype.listenerCount = function(type) {
    return this.listeners(type).length;
  };

  EventEmitter.prototype.listenersAny = function() {

    if(this._all) {
      return this._all;
    }
    else {
      return [];
    }

  };

  if (true) {
     // AMD. Register as an anonymous module.
    !(__WEBPACK_AMD_DEFINE_RESULT__ = function() {
      return EventEmitter;
    }.call(exports, __webpack_require__, exports, module),
				__WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));
  } else if (typeof exports === 'object') {
    // CommonJS
    module.exports = EventEmitter;
  }
  else {
    // Browser global.
    window.EventEmitter2 = EventEmitter;
  }
}();

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(1)))

/***/ }),
/* 8 */
/***/ (function(module, exports) {

throw new Error("Module build failed: TypeError: /Users/tim/Desktop/git-repos/gesture-helper/src/inputdevicecapabilities-polyfill.js: Property init of VariableDeclarator expected node to be of a type [\"Expression\"] but instead got \"ObjectMethod\"\n    at Object.validate (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-types/lib/definitions/index.js:109:13)\n    at validate (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-types/lib/index.js:505:9)\n    at Object.builder (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-types/lib/index.js:466:7)\n    at PathHoister.run (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/path/lib/hoister.js:194:24)\n    at NodePath.hoist (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/path/modification.js:263:18)\n    at PluginPass.Function (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-plugin-transform-hoist-nested-functions/lib/index.js:101:14)\n    at newFn (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/visitors.js:276:21)\n    at NodePath._call (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/path/context.js:76:18)\n    at NodePath.call (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/path/context.js:48:17)\n    at NodePath.visit (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/path/context.js:105:12)\n    at TraversalContext.visitQueue (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/context.js:150:16)\n    at TraversalContext.visitMultiple (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/context.js:103:17)\n    at TraversalContext.visit (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/context.js:190:19)\n    at Function.traverse.node (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/index.js:114:17)\n    at NodePath.visit (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/path/context.js:115:19)\n    at TraversalContext.visitQueue (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/context.js:150:16)\n    at TraversalContext.visitSingle (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/context.js:108:19)\n    at TraversalContext.visit (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/context.js:192:19)\n    at Function.traverse.node (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/index.js:114:17)\n    at NodePath.visit (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/path/context.js:115:19)\n    at TraversalContext.visitQueue (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/context.js:150:16)\n    at TraversalContext.visitSingle (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/context.js:108:19)\n    at TraversalContext.visit (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/context.js:192:19)\n    at Function.traverse.node (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/index.js:114:17)\n    at NodePath.visit (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/path/context.js:115:19)\n    at TraversalContext.visitQueue (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/context.js:150:16)\n    at TraversalContext.visitMultiple (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/context.js:103:17)\n    at TraversalContext.visit (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/context.js:190:19)\n    at Function.traverse.node (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/index.js:114:17)\n    at NodePath.visit (/Users/tim/Desktop/git-repos/gesture-helper/node_modules/babel-traverse/lib/path/context.js:115:19)");

/***/ })
/******/ ])["default"];
});